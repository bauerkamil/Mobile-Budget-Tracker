import { StyleSheet } from "react-native";

export const CategoryStyle = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    flex: 1,
    flexDirection: "row",
    alignItems: "center",
    columnGap: 10,
    padding: 5,
    borderRadius: 20,
  },
  icon: {
    padding: 10,
    borderRadius: 20,
  },
});

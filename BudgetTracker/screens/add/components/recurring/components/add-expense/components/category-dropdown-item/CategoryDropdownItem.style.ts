import { StyleSheet } from "react-native";

export const CategoryDropdownItemStyle = StyleSheet.create({
  content: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "flex-start",
    gap: 10,
  },
});

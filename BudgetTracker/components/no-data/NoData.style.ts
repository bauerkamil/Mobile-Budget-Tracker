import { StyleSheet } from 'react-native';

export const NoDataStyles = StyleSheet.create({
  image: {
    width: 150,
    height: 150,
  },
  header: {
    fontSize: 20,
    fontWeight: "bold",
  }
});
